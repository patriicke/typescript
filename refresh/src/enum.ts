//Here enums are user defined variables here I'll use the size as an example
//Remember that we use enum keyword
//We also use Pascal naming convention

enum Size {
  Small = 1,
  Medium,
  Large
}

const mySize: Size = Size.Medium;
console.log(mySize);
